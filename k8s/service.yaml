apiVersion: v1
kind: Service
metadata:
  name: gmail-mcp-server
  namespace: gmail-dashboard
spec:
  selector:
    app: gmail-mcp-server
  ports:
    - port: 5000
      targetPort: 5000
  type: LoadBalancer
